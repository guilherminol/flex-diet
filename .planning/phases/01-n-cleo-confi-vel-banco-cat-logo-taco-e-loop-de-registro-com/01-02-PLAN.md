---
phase: 01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/db/seed/pof_medidas_caseiras.csv
  - src/db/seed/seed.ts
  - src/domain/alimentos.ts
  - src/mcp/tools/catalogo.ts
  - src/mcp/server.ts
  - tests/seed.test.ts
  - tests/alimentos.test.ts
autonomous: true
requirements: [ALIM-06]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "buscar_alimento('banana') retorna até 5 candidatos com kcal/proteina/carbo/gordura POR 100g e Banana maçã entre eles (86–89 kcal/100g) (ALIM-06)"
    - "Alimentos com match POF↔TACO inequívoco retornam medidas_caseiras com descricao + gramas (ex.: óleo de soja); alimentos sem match inequívoco retornam medidas_caseiras: [] — sem fuzzy-match inventado (RESEARCH Pitfall 3)"
    - "O seed loga no boot quantos alimentos TACO ficaram com >= 1 medida caseira (esperado ~82; cobertura parcial aceita — D-04 manda o Hermes estimar e declarar a premissa no resto)"
    - "Termo de busca com % ou _ não vira wildcard — caracteres escapados e query 100% por prepared statement"
    - "Busca sem resultado retorna lista vazia estruturada (não exceção); alimento_id inexistente em registrar_refeicao continua retornando erro alimento_nao_encontrado com sugestão de usar buscar_alimento"
  artifacts:
    - path: "src/db/seed/pof_medidas_caseiras.csv"
      provides: "Medidas caseiras POF/IBGE vendidas no repo (brolesi/taco v1.7.0)"
    - path: "src/db/seed/seed.ts"
      provides: "Join POF→TACO por nome-base INEQUÍVOCO alimentando medida_caseira + log de cobertura"
    - path: "src/domain/alimentos.ts"
      provides: "buscarAlimentos(termo) — LIKE normalizado com escape, top-5, medidas agregadas"
    - path: "src/mcp/tools/catalogo.ts"
      provides: "Tool buscar_alimento (readOnlyHint) com eco por 100g + medidas_caseiras"
  key_links:
    - from: "src/mcp/tools/catalogo.ts"
      to: "src/domain/alimentos.ts"
      via: "tool chama buscarAlimentos e devolve structuredContent com candidatos e medidas"
      pattern: "buscarAlimentos"
    - from: "src/domain/alimentos.ts"
      to: "src/db/migrations/001_init.sql"
      via: "JOIN alimento ↔ medida_caseira para anexar equivalências a cada candidato"
      pattern: "medida_caseira"
---

<objective>
Completar o catálogo: busca de alimentos (`buscar_alimento`) com valores por 100g e as equivalências de medidas caseiras em gramas (ALIM-06), via join conservador POF↔TACO por nome-base inequívoco.

Purpose: O Hermes precisa desta tool para resolver "almocei arroz, feijão e frango" em alimento_ids e para declarar a premissa de gramas quando a mensagem não traz quantidade (D-04).
Output: Tool buscar_alimento ativa; seed enriquecido com medidas caseiras (~82 alimentos) e cobertura logada; testes de busca e de join.

## Phase Goal

**As a** usuário do Flex Diet, **I want to** buscar um alimento e ver os valores por 100g e as medidas caseiras em gramas, **so that** o que registro reflete o que realmente comi.
</objective>

<execution_context>
@C:/Users/guilhermino.araujo/.zcode/gsd-core/workflows/execute-plan.md
@C:/Users/guilhermino.araujo/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-CONTEXT.md
@.planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-RESEARCH.md
@.planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Medidas caseiras POF no seed — join conservador por nome-base inequívoco</name>
  <precondition>rede disponível para clonar github.com/brolesi/taco (dataset público usado no research; clone raso basta)</precondition>
  <files>src/db/seed/pof_medidas_caseiras.csv, src/db/seed/seed.ts, tests/seed.test.ts</files>
  <read_first>
    src/db/seed/seed.ts
    src/db/migrations/001_init.sql
    .planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-RESEARCH.md
  </read_first>
  <action>
    1. Clonar `git clone --depth 1 https://github.com/brolesi/taco` em diretório temporário; copiar `data/processed/pof/pof_medidas_caseiras.csv` para `src/db/seed/pof_medidas_caseiras.csv`; remover o clone. Antes de mapear, INSPECIONAR o header do CSV vendido (o dicionário do dataset documenta as colunas; RESEARCH registra ~11.8k linhas POF).
    2. Estender `src/db/seed/seed.ts`: parsear o CSV POF com csv-parse/sync; derivar **nome-base** de cada alimento POF (parte do nome antes da primeira vírgula, lowercase, trim) e o nome-base de cada alimento TACO já mapeado (mesma normalização); para cada nome-base POF, localizar alimentos TACO com o MESMO nome-base: **só inserir medidas quando existir EXATAMENTE UM alimento TACO com aquele nome-base** (join inequívoco — nunca fuzzy, nunca múltiplos candidatos; RESEARCH Pitfall 3: só ~82 alimentos são inequívocos e o dataset documenta por que um casamento amplo vira erro silencioso de nutriente). Inserir em medida_caseira (alimento_id, descricao da medida, gramas) com upsert idempotente por UNIQUE(alimento_id, descricao), dentro da transação síncrona do seed. Valores de gramas vazios/inválidos são pulados.
    3. Retornar do seed e logar no boot (stderr): contagem de alimentos TACO com >= 1 medida e total de linhas de medida inseridas. Cobertura parcial é resultado VÁLIDO e esperado — o log existe para medir, não para prometer 100%.
    4. Estender `tests/seed.test.ts`: (a) pelo menos um alimento TACO conhecido com match inequívoco (ex.: 'Óleo, de soja') tem >= 1 medida caseira com gramas > 0; (b) contagem de alimentos com medidas é > 0 e <= 200 (guarda contra join explosivo/fuzzy); (c) re-seed não duplica medidas (contagem estável).
  </action>
  <verify>
    <automated>npx vitest run tests/seed.test.ts</automated>
    <fails_when>exit não-zero, ou qualquer teste "failed", ou "0 passed" no resumo do vitest</fails_when>
  </verify>
  <verify>
    <automated>npm run check</automated>
    <fails_when>exit não-zero (erros de tipo ou lint)</fails_when>
  </verify>
  <done>CSV POF vendido; seed popula medida_caseira apenas para matches inequívocos, loga a cobertura e é idempotente; testes de sanity do seed continuam verdes.</done>
  <acceptance_criteria>
    - `src/db/seed/pof_medidas_caseiras.csv` existe e é não-vazio (vendido do brolesi/taco)
    - `src/db/seed/seed.ts` contém a normalização de nome-base e a condição de unicidade (insere medida só com EXATAMENTE 1 alimento TACO de mesmo nome-base)
    - Log do boot imprime a contagem de alimentos com medidas
    - tests/seed.test.ts prova: óleo de soja com medidas, contagem dentro do teto, idempotência
    - Suíte vitest verde
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Tool buscar_alimento — top-5 por 100g + medidas caseiras</name>
  <files>src/domain/alimentos.ts, src/mcp/tools/catalogo.ts, src/mcp/server.ts, tests/alimentos.test.ts</files>
  <read_first>
    src/mcp/server.ts
    src/mcp/tools/saldo.ts
    src/db/seed/seed.ts
    .planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-PATTERNS.md
  </read_first>
  <action>
    1. **src/domain/alimentos.ts**: `buscarAlimentos(db, termo)` — normalizar termo (trim, lowercase), escapar os caracteres curinga de LIKE (`%` e `_`) do termo do usuário, prepared statement com LIKE (case-insensitive via lower()) em alimento.nome ordenado por nome, LIMIT 5; para cada candidato, anexar `medidas_caseiras: [{ descricao, gramas }]` de medida_caseira (ordem estável por descricao). Retornar também kcal/proteina_g/carbo_g/gordura_g POR 100g e a `fonte` de cada alimento.
    2. **src/mcp/tools/catalogo.ts**: tool `buscar_alimento` com inputSchema zod (`termo` string min 2 max 100), annotation readOnlyHint true, resposta structuredContent no padrão D-05. Zero resultados → resposta normal com lista vazia (não erro — quem decide o que fazer é o Hermes).
    3. **src/mcp/server.ts**: registrar buscar_alimento no factory.
    4. **tests/alimentos.test.ts**: (a) busca 'banana' retorna >= 1 candidato e inclui Banana maçã com kcal entre 86 e 89 por 100g; (b) candidato com match POF traz >= 1 medida com gramas; (c) termo contendo '%' não expande a busca para tudo (retorna vazio ou match literal — nunca a tabela inteira); (d) termos com menos de 2 caracteres são rejeitados pelo schema; (e) busca sem resultado retorna lista vazia sem erro; (f) buscarAlimentos usa prepared statement (assert de fonte: nenhuma concatenação de string no SQL — inspeção de código no teste via leitura do arquivo ou por execução com termo malicioso que não corrompe a query).
  </action>
  <verify>
    <automated>npx vitest run tests/alimentos.test.ts</automated>
    <fails_when>exit não-zero, ou qualquer teste "failed", ou "0 passed" no resumo do vitest</fails_when>
  </verify>
  <verify>
    <automated>npx vitest run</automated>
    <fails_when>exit não-zero, ou "failed" maior que zero, ou "0 passed" no resumo</fails_when>
  </verify>
  <verify>
    <automated>npm run check</automated>
    <fails_when>exit não-zero (erros de tipo ou lint)</fails_when>
  </verify>
  <done>buscar_alimento responde candidatos com macros por 100g e medidas caseiras em gramas quando existem; wildcard do LIKE neutralizado; suíte completa verde.</done>
  <acceptance_criteria>
    - `src/domain/alimentos.ts` contém escape de % e _ do termo antes do LIKE e uso exclusivo de prepared statement
    - `src/mcp/tools/catalogo.ts` registra a tool buscar_alimento com readOnlyHint e structuredContent
    - tests/alimentos.test.ts cobre: banana por 100g (86–89), medidas agregadas ao candidato, neutralização de wildcard, lista vazia sem erro
    - Suíte completa vitest verde
  </acceptance_criteria>
</task>

</tasks>

## Flagged Assumptions (edge-probe, não resolvidos — nunca drop silencioso)

| Requisito | Assunção explícita desta fase |
|---|---|
| ALIM-06 | Cobertura do join é PARCIAL por design (~82 alimentos inequívocos do dataset); o caso "sem medida" é contrato de primeira classe (medidas_caseiras: []) e o Hermes compensa estimando com premissa declarada (D-04) — nenhuma edge de "medida errada" além do guard de unicidade do join. |

## Artifacts this phase produces

Símbolos criados por ESTE plano (complementam os do plan 01):
- **Arquivos**: `src/db/seed/pof_medidas_caseiras.csv`, `src/domain/alimentos.ts`, `src/mcp/tools/catalogo.ts`, `tests/alimentos.test.ts`
- **Funções/exports**: `buscarAlimentos`
- **Tools MCP**: `buscar_alimento` (total da fase: 4 de 8)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hermes → termo de busca → SQL LIKE | Texto livre do LLM entra em query de texto |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Tampering | buscar_alimento → LIKE | high | mitigate | Escape de % e _ do termo + prepared statement; testado com termo malicioso em tests/alimentos.test.ts |
| T-01-08 | DoS | termo gigante | low | mitigate | zod min 2 / max 100 no inputSchema |
| T-01-09 | Tampering | Join POF→TACO silenciosamente errado | medium | mitigate | Join apenas por nome-base INEQUÍVOCO (guard de unicidade no seed); medidas_caseiras: [] no resto — sem fuzzy-match |
</threat_model>

<verification>
- `npx vitest run` — suíte inteira verde (inclui seed com join e alimentos)
- `npm run check` — tsc + biome sem erros
- Boot do servidor loga a cobertura de medidas (contagem ~82 esperada, valor real registrado no SUMMARY)
</verification>

<success_criteria>
- Hermes consegue resolver nomes → alimento_ids e ver equivalências de medidas caseiras para declarar premissas de gramas (D-04)
- Nenhum alimento recebe medida caseira inventada por casamento ambíguo
- Critério 3 do ROADMAP (busca com valores por 100g + equivalências + sanity TACO) coberto pelos testes
</success_criteria>

<output>
Create `.planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-02-SUMMARY.md` when done
</output>
