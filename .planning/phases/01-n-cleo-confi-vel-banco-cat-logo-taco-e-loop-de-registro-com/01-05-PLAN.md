---
phase: 01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com
plan: 05
type: execute
wave: 4
depends_on: ["01-03", "01-04"]
files_modified:
  - tests/client-e2e.test.ts
  - docs/sdk-v1-fallback.md
autonomous: false
user_setup:
  - service: Hermes (cliente MCP, fora deste repo)
    why: "INFRA-01: o Hermes precisa apontar ao servidor por URL com Bearer token para o 1º contato real — configuração é no cliente, fora do alcance do executor"
    env_vars: []
    dashboard_config:
      - task: "Configurar no Hermes o servidor MCP por URL (http(s)://<vps>:<porta>/mcp) com Authorization: Bearer <MCP_TOKEN>"
        location: "Config do Hermes (mantido fora deste repositório)"
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Um cliente MCP real conversa com o servidor por Streamable HTTP + Bearer: initialize → tools/list (8 tools) → tools/call registrar_refeicao → eco + saldo — tudo automatizado em teste (INFRA-01)"
    - "Fallback SDK v1 documentado em docs/sdk-v1-fallback.md: condição de acionamento (Hermes negocia era anterior), passo de pinning (@modelcontextprotocol/sdk 1.30.1) e o que NÃO muda (shape das 8 tools, dedupe, saldo)"
    - "A era do protocolo que o Hermes negocia no 1º contato fica registrada no doc (v2 confirmado ou fallback v1 aplicado)"
    - "Primeiro registro real via WhatsApp devolve pelo Hermes o eco item + gramas + fonte + saldo do dia (critério 1 do ROADMAP, lado humano)"
    - "Reenvio da mesma mensagem no WhatsApp não duplica registro (duplicado: true informado pelo servidor, comunicável pelo Hermes)"
  artifacts:
    - path: "tests/client-e2e.test.ts"
      provides: "Cliente JSON-RPC completo (initialize/tools/list/tools/call) sobre HTTP + Bearer contra o servidor real em porta efêmera"
    - path: "docs/sdk-v1-fallback.md"
      provides: "Plano de fallback SDK v1 + registro da era negociada pelo Hermes"
  key_links:
    - from: "tests/client-e2e.test.ts"
      to: "src/server.ts"
      via: "startServer em porta efêmera + fetch autenticado com MCP_TOKEN de teste"
      pattern: "startServer"
    - from: "docs/sdk-v1-fallback.md"
      to: "package.json"
      via: "fallback = pinar @modelcontextprotocol/sdk 1.30.1 mantendo o desenho das tools"
      pattern: "@modelcontextprotocol"
---

<objective>
Fechar INFRA-01 de ponta a ponta: provar o transporte com um cliente MCP real (não só curl), documentar o fallback SDK v1, e fazer o 1º contato com o Hermes — URL + token na VPS, era do protocolo verificada, primeiro registro real via WhatsApp.

Purpose: O servidor só vale quando o Hermes o consome; esta plan transforma "API pronta" em "produto usável no WhatsApp" e encerra a única incógnita restante (era do SDK).
Output: Teste e2e de cliente MCP verde; fallback documentado; era do Hermes registrada; loop real funcionando no WhatsApp.

## Phase Goal

**As a** usuário do Flex Diet, **I want to** mandar o que comi no WhatsApp e receber o saldo restante do dia, **so that** o sistema esteja vivo no meu canal real, não só em testes.
</objective>

<execution_context>
@C:/Users/guilhermino.araujo/.zcode/gsd-core/workflows/execute-plan.md
@C:/Users/guilhermino.araujo/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-CONTEXT.md
@.planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-RESEARCH.md
@.planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cliente MCP end-to-end automatizado (initialize → tools/list → tools/call)</name>
  <files>tests/client-e2e.test.ts</files>
  <read_first>
    src/server.ts
    src/mcp/server.ts
    tests/http.test.ts
    .planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-RESEARCH.md
  </read_first>
  <action>
    1. **tests/client-e2e.test.ts**: subir o servidor com startServer({ port: 0 }) e MCP_TOKEN de teste; sobre HTTP puro com fetch, executar o handshake JSON-RPC completo: (a) POST /mcp com Bearer correto e Accept application/json, text/event-stream, method initialize (clientInfo + protocolVersion recente) — capturar serverInfo/capabilities e, SE a resposta trouxer header de sessão, ecoá-lo nas chamadas seguintes; (b) method tools/list → deve listar EXATAMENTE as 8 tools da fase (registrar_refeicao, consultar_saldo, buscar_alimento, listar_registros, editar_registro, remover_registro, repetir_refeicao, definir_metas); (c) chamar definir_metas com os 4 valores e registrar_refeicao com 120g da banana maçã → structuredContent com eco + saldo status ok; (d) repetir a tools/call idêntica → duplicado: true e nenhum segundo registro (idempotência visível por cliente, não só por domínio); (e) sem Bearer → 401 no initialize.
    2. Se o SDK expor uma sessão/statefulness inesperada no modo stateless (ex.: initialize obrigatório por conexão), registrar o comportamento observado no SUMMARY — é dado para o checkpoint do Hermes, não motivo para mudar a arquitetura.
    3. Manter o teste autocontido: banco em tmpdir (FLEXDIET_DB_PATH), porta efêmera, token de teste — nada de rede externa.
  </action>
  <verify>
    <automated>npx vitest run tests/client-e2e.test.ts</automated>
    <fails_when>exit não-zero, ou qualquer teste "failed", ou "0 passed" no resumo do vitest</fails_when>
  </verify>
  <verify>
    <automated>npx vitest run</automated>
    <fails_when>exit não-zero, ou "failed" maior que zero, ou "0 passed" no resumo</fails_when>
  </verify>
  <verify>
    <automated>npm run check</automated>
    <fails_when>exit não-zero (erros de tipo ou lint)</fails_when>
  </verify>
  <done>Cliente JSON-RPC completo prova o transporte Streamable HTTP + Bearer com as 8 tools e idempotência — INFRA-01 provado sem depender do Hermes.</done>
  <acceptance_criteria>
    - tests/client-e2e.test.ts executa initialize, tools/list (assert das 8 tools nome a nome), tools/call de definir_metas + registrar_refeicao, reenvio com duplicado: true, e 401 sem token
    - Suíte completa vitest verde
    - SUMMARY registra qualquer comportamento de sessão observado do SDK (para o checkpoint)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fallback SDK v1 documentado (INFRA-01, critério 5 do ROADMAP)</name>
  <files>docs/sdk-v1-fallback.md</files>
  <read_first>
    package.json
    src/mcp/server.ts
    .planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-RESEARCH.md
  </read_first>
  <action>
    1. Criar **docs/sdk-v1-fallback.md** com: (a) QUANDO acionar — o Hermes negocia apenas era de protocolo anterior à suportada pelo SDK v2 no primeiro contato (verificação do checkpoint da Task 3); (b) COMO — pinar `@modelcontextprotocol/sdk` 1.30.1 (v1), trocar os imports de McpServer/registerTool e o transporte HTTP para os equivalentes da v1, mantendo zod e o inputSchema das tools; (c) O QUE NÃO MUDA — nomes/shape das 8 tools, JSON estruturado (D-05), dedupe (D-07), saldo único (INFRA-03), migrations; (d) checklist de reversão de volta à v2 quando o Hermes atualizar; (e) seção "Era negociada pelo Hermes" com placeholder a ser preenchido no checkpoint (data + era observada + decisão).
    2. Basear o doc nos fatos do RESEARCH (v1 1.30.1 recebe fixes >= 6 meses pós-v2; registerTool existe na v1 — tools quase não mudam). Sem preencher a era observada ainda — isso acontece no 1º contato real.
  </action>
  <verify>
    <automated>test -f docs/sdk-v1-fallback.md && grep -c "1.30.1" docs/sdk-v1-fallback.md</automated>
    <fails_when>exit não-zero (doc ausente ou sem a versão de pinning da v1)</fails_when>
  </verify>
  <done>Fallback documentado: condição, passo a passo de pinning, invariantes preservados e lugar para registrar a era do Hermes.</done>
  <acceptance_criteria>
    - docs/sdk-v1-fallback.md existe com: condição de acionamento, pinning @modelcontextprotocol/sdk 1.30.1, invariants (tools/saldo/dedupe) e seção "Era negociada pelo Hermes"
    - Doc não promete cobertura que o RESEARCH não verificou (era do Hermes fica pendente ao checkpoint)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: 1º contato com o Hermes — URL + token, era do SDK, registro real no WhatsApp</name>
  <precondition>VPS com o serviço no ar e /healthz respondendo (deploy do plan 04 concluído); MCP_TOKEN da VPS disponível com a pessoa</precondition>
  <files></files>
  <read_first>
    docs/sdk-v1-fallback.md
    .planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-04-SUMMARY.md
  </read_first>
  <what-built>Integração final do Flex Diet com o canal real: cliente MCP provado em teste, fallback SDK v1 documentado, checklist de configuração do Hermes pronto — falta o 1º contato real (Hermes configurado pela pessoa + mensagens no WhatsApp)</what-built>
  <how-to-verify>
    Executor prepara antes do checkpoint: (1) checklist exato de configuração do Hermes (URL http(s)://&lt;vps&gt;:&lt;porta&gt;/mcp; header Authorization: Bearer &lt;MCP_TOKEN&gt;; Accept application/json, text/event-stream); (2) se o Hermes expor logs, o que procurar (era do protocolo no initialize; 401 = token errado; 403 = allowedHosts/PUBLIC_HOST errado).

    A pessoa configura o Hermes e responde:
    1. Mensagem real no WhatsApp ("comi 120g de banana maçã") → resposta traz item + gramas + fonte + saldo do dia?
    2. "meta 1800kcal 150p 180c 60g" (ou equivalente que leve o Hermes a coletar os 4 valores, D-10) → saldo recalcula?
    3. Reenviar a mesma mensagem de registro → o Hermes comunica "já registrado" e nada duplica?
    4. Que ERA de protocolo o Hermes negociou (v2 confirmada ou fallback v1 acionado)? Se fallback: aplicar o docs/sdk-v1-fallback.md e revalidar as perguntas 1-3.

    Executor pós-checkpoint: preencher a seção "Era negociada pelo Hermes" do doc com a resposta (e o passo de fallback executado, se for o caso); registrar no SUMMARY o resultado do 1º contato e a decisão de TLS (se a VPS já tem reverse proxy com TLS, expor por HTTPS; senão HTTP + token + firewall permanece o MVP aceito, com o risco T-01-15 anotado).
  </how-to-verify>
  <resume-signal>Responder "aprovado" com a era negociada pelo Hermes (item 4) confirmando os itens 1-3, ou descrever o que falhou (token/401, allowedHosts/403, era incompatível — executor segue o plano de fallback ou instrui a correção)</resume-signal>
  <verify>
    <automated>grep -c "Era negociada pelo Hermes" docs/sdk-v1-fallback.md</automated>
    <fails_when>nenhuma linha casada (seção não preenchida após o checkpoint)</fails_when>
  </verify>
  <done>Primeiro registro real via WhatsApp com eco + saldo; reenvio não duplica; era do SDK registrada (v2 ou fallback aplicado); decisão de TLS anotada no SUMMARY.</done>
  <acceptance_criteria>
    - Seção "Era negociada pelo Hermes" preenchida no docs/sdk-v1-fallback.md com data e era observada (ou fallback aplicado e revalidado)
    - SUMMARY registra: resultado das 3 perguntas do WhatsApp e a decisão de TLS
    - Nenhum segredo (token real) escrito em nenhum arquivo versionado
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

Símbolos criados por ESTE plano (complementam os dos plans 01–04):
- **Arquivos**: `tests/client-e2e.test.ts`, `docs/sdk-v1-fallback.md`
- **Símbolos**: nenhum export novo de produção (prova de integração + documentação de contingência)
- **Registro de decisão**: era do protocolo do Hermes + decisão de TLS (no doc e no SUMMARY)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config do Hermes → endpoint público | URL/token configurados fora do repositório |
| WhatsApp → Hermes → /mcp | Mensagem real atravessa o canal de produção |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-18 | Spoofing | Config errada do Hermes expondo endpoint sem proteção efetiva | medium | mitigate | Checkpoint confirma 401 sem token e allowedHosts com PUBLIC_HOST correto; token de alta entropia já exigido no setup |
| T-01-19 | Info Disclosure | Token/segredos em arquivos versionados durante o 1º contato | high | mitigate | Token real só na config do Hermes e no compose.env da VPS; nenhum arquivo versionado o recebe (criterio de aceitação da Task 3) |
| T-01-20 | Tampering | Fallback v1 aplicado de forma parcial (server v2, doc v1) | low | mitigate | Doc tem checklist de troca completa de pacote + revalidação das 3 perguntas do WhatsApp antes de declarar pronto |
</threat_model>

<verification>
- `npx vitest run` — suíte completa verde, incluindo o cliente e2e com as 8 tools
- docs/sdk-v1-fallback.md com a era do Hermes preenchida pós-checkpoint
- Critério 5 do ROADMAP (era v2 vs v1 verificada no 1º contato, com fallback documentado) fechado
</verification>

<success_criteria>
- INFRA-01 completo: servidor consumível por URL com token, provado por cliente real e pelo Hermes
- Única incógnita restante da fase (era do SDK) resolvida e registrada
- Critério 1 do ROADMAP observado no canal real (WhatsApp): registro → eco item+gramas+fonte+saldo
</success_criteria>

<output>
Create `.planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-05-SUMMARY.md` when done
</output>
