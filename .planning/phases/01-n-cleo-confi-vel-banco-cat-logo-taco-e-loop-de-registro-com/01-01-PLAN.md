---
phase: 01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - package.json
  - package-lock.json
  - tsconfig.json
  - vitest.config.ts
  - biome.json
  - src/lib/datas.ts
  - src/db/connect.ts
  - src/db/migrate.ts
  - src/db/migrations/001_init.sql
  - src/db/seed/seed.ts
  - src/db/seed/taco_composicao.csv
  - src/domain/saldo.ts
  - src/domain/refeicoes.ts
  - src/domain/metas.ts
  - src/mcp/server.ts
  - src/mcp/tools/registrar.ts
  - src/mcp/tools/saldo.ts
  - src/mcp/tools/metas.ts
  - src/server.ts
  - tests/datas.test.ts
  - tests/migrations.test.ts
  - tests/seed.test.ts
  - tests/http.test.ts
  - tests/metas.test.ts
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-03, INFRA-04, ALIM-01, REG-01, REG-02, REG-05, META-01]
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "POST /mcp sem Bearer token responde 401; com MCP_TOKEN válido, initialize e tools/list respondem (INFRA-01)"
    - "registrar_refeicao com itens {alimento_id, gramas} debita kcal/macros do catálogo TACO e retorna na MESMA resposta o eco (nome + gramas + fonte taco + macros), id_curto e o saldo do dia (D-03/D-05, REG-01/REG-02)"
    - "150g de 'Arroz, tipo 2, cozido' (130,120 kcal/100g no CSV, numero_alimento 5) debitam ~195,18 kcal no saldo"
    - "Duas chamadas concorrentes de registro aplicam cada débito atomicamente (transação SQLite): cada chamada debita por completo ou não debita nada — o saldo nunca reflete débito parcial"
    - "db.pragma('journal_mode') retorna 'wal' após o connect; migrations versionadas avançam user_version no boot e cada migration pendente gera backup prévio em data/backups/; rodar o boot de novo é no-op idempotente (INFRA-02)"
    - "Todo insert grava timestamp_utc (ISO-8601 UTC) e data_local (YYYY-MM-DD, America/Sao_Paulo); 2026-09-24T02:50:00Z resolve data_local 2026-09-23 e 2026-09-24T03:10:00Z resolve 2026-09-24 (INFRA-03)"
    - "Registro com parâmetro data retroativo grava na data_local informada, não na data de agora (REG-05)"
    - "Seed TACO: banana maçã (numero_alimento 178) entre 85 e 89 kcal/100g — nunca ~363, que indicaria leitura de kJ; 'Óleo, de soja' entre 880 e 888; nenhum alimento com kcal > 950 exceto os de gordura >= 90g/100g (ALIM-01)"
    - "Saldo sem metas definidas responde status meta_nao_definida + instrução de como definir, nunca números inventados (D-09)"
    - "definir_metas exige os 4 valores (kcal, proteina_g, carbo_g, gordura_g); o schema zod rejeita chamada com apenas 3; após definir, o saldo recalcula na mesma resposta (D-10, META-01)"
    - "Respostas das tools são JSON estruturado (structuredContent), nunca texto pronto pra repassar (D-05)"
  artifacts:
    - path: "src/server.ts"
      provides: "Entrypoint: /mcp com requireBearerAuth + createMcpHandler stateless + /healthz; startServer() exportado p/ testes; boot roda migrations e seed"
    - path: "src/mcp/server.ts"
      provides: "Factory buildServer (McpServerFactory) registrando registrar_refeicao, consultar_saldo, definir_metas"
    - path: "src/domain/saldo.ts"
      provides: "calcularSaldo — ÚNICA implementação do saldo, derivado em leitura por data_local"
    - path: "src/domain/refeicoes.ts"
      provides: "registrarRefeicao transacional com snapshot de macros + gerarIdCurto (4 hex, retry em colisão)"
    - path: "src/domain/metas.ts"
      provides: "definirMetas (4 valores obrigatórios) + obterMetasVigentes por data_inicio"
    - path: "src/db/migrations/001_init.sql"
      provides: "Tabelas alimento, medida_caseira, refeicao, refeicao_item, meta_diaria"
    - path: "src/db/migrate.ts"
      provides: "Runner PRAGMA user_version + await db.backup() pré-migration (fora de transação)"
    - path: "src/db/connect.ts"
      provides: "Conexão better-sqlite3 com WAL + busy_timeout + foreign_keys; caminho via FLEXDIET_DB_PATH"
    - path: "src/db/seed/seed.ts"
      provides: "Seed TACO idempotente (upsert por fonte+numero_taco), leitura exclusiva de energia_kcal, saneamento vazio=NULL / Tr=0, sanity in-code"
    - path: "src/db/seed/taco_composicao.csv"
      provides: "Dataset TACO vendido no repo (brolesi/taco v1.7.0)"
    - path: "src/lib/datas.ts"
      provides: "agoraIsoUtc, dataLocalSp, hojeLocalSp, resolverDataLocal (America/Sao_Paulo)"
  key_links:
    - from: "src/server.ts"
      to: "src/mcp/server.ts"
      via: "createMcpHandler(buildServer) atrás de requireBearerAuth na rota /mcp; req.body passado como 3º argumento do node handler"
      pattern: "createMcpHandler"
    - from: "src/mcp/tools/registrar.ts"
      to: "src/domain/refeicoes.ts"
      via: "tool chama registrarRefeicao e devolve structuredContent com eco + id_curto + saldo"
      pattern: "registrarRefeicao"
    - from: "src/domain/saldo.ts"
      to: "src/db/migrations/001_init.sql"
      via: "agregação SUM sobre refeicao_item JOIN refeicao por data_local, com metas vigentes de meta_diaria"
      pattern: "refeicao_item"
  prohibitions:
    - statement: "Nenhum dado pessoal sai da VPS nesta fase: o processo não faz nenhuma chamada de rede de saída (sem telemetria, analytics, error reporting ou cliente HTTP externo); a única requisição de saída permitida é o healthcheck do compose apontando ao próprio /healthz"
      status: resolved
      verification: judgment
      category: privacy
    - statement: "O servidor nunca aplica default, sugestão ou ajuste automático de metas; metas mudam somente por chamada explícita de definir_metas com os 4 valores — a instrução estática de COMO definir (D-09) não é sugestão de valores"
      status: resolved
      verification: judgment
      category: values
---

<objective>
Walking Skeleton da Phase 1: provar de ponta a ponta o circuito "registrei o que comi → recebi o saldo restante de macros" — servidor MCP em TypeScript (Streamable HTTP + Bearer token), SQLite em WAL com migrations versionadas + backup pré-migration, catálogo TACO semeado com teste de sanidade, datas duplas `timestamp_utc`+`data_local`, e as três tools de base (`registrar_refeicao`, `consultar_saldo`, `definir_metas`).

Purpose: É a prova de que a arquitetura inteira funciona (HTTP → MCP tool → domínio → SQLite) antes de expandir; cada decisão aqui vira o padrão que as fases 2–5 copiam.
Output: Servidor rodando localmente com 3 tools, suíte vitest verde (http, seed, migrations, datas, metas) e a fundação de deploy (gitignore-first) já no lugar.

## Phase Goal

**As a** usuário do Flex Diet no WhatsApp, **I want to** mandar o que comi e receber de volta o saldo restante de calorias e macros do dia, **so that** eu acompanhe minha dieta com números confiáveis e atrito mínimo.

*Nota: o `**Goal:**` do ROADMAP estava em prosa; a história acima é reformatação 1:1 do mesmo conteúdo (ator/capacidade/outcome já estavam lá). Não muda escopo nem particionamento — se quiser o split formal de SPIDR, rodar `/gsd mvp-phase 1`, mas a fatia já é vertical e única.*

*Aceite consciente (revisão iteração 2, warning scope_sanity): os 25 arquivos declarados excedem de propósito o limiar de contagem por plan — este plan É a fatia tracer única do Walking Skeleton (mandato do MVP), e fatiar o tracer quebraria a prova arquitetural ponta a ponta. A escape registrada, caso pressão de contexto apareça na execução, é mover a Task 2 (definir_metas) para um plan companheiro — nunca dividir o tracer.*
</objective>

<execution_context>
@C:/Users/guilhermino.araujo/.zcode/gsd-core/workflows/execute-plan.md
@C:/Users/guilhermino.araujo/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-CONTEXT.md
@.planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-RESEARCH.md
@.planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — "registrei → saldo" de ponta a ponta (HTTP MCP + SQLite WAL + seed TACO + datas duplas)</name>
  <reversibility rating="one-way">D-01 (TypeScript/Node) e o contrato das tools (D-03..D-06) são publicados com o Hermes — porta já atravessada com decisão registrada do usuário em CONTEXT.md; rating mantido para registro, sem checkpoint de re-confirmação.</reversibility>
  <files>.gitignore, package.json, package-lock.json, tsconfig.json, vitest.config.ts, biome.json, src/lib/datas.ts, src/db/connect.ts, src/db/migrate.ts, src/db/migrations/001_init.sql, src/db/seed/seed.ts, src/db/seed/taco_composicao.csv, src/domain/saldo.ts, src/domain/refeicoes.ts, src/mcp/server.ts, src/mcp/tools/registrar.ts, src/mcp/tools/saldo.ts, src/server.ts, tests/datas.test.ts, tests/migrations.test.ts, tests/seed.test.ts, tests/http.test.ts</files>
  <read_first>
    .planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-PATTERNS.md
    .planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-RESEARCH.md
    .github/workflows/deploy.yml
  </read_first>
  <action>
    Ordem obrigatória dentro da task: .gitignore ANTES do primeiro commit de código (dados de saúde nunca no git).

    1. **.gitignore** (primeiro arquivo): `node_modules/`, `data/`, `compose.env`, `*.db`, `*.db-wal`, `*.db-shm`, `dist/`.
    2. **Gate de legitimidade pré-install** (obrigação da Package Legitimacy Audit do RESEARCH): rodar `npm view @modelcontextprotocol/server version` e confirmar via fetch do README de github.com/modelcontextprotocol/typescript-sdk que o nome do pacote v2 ainda é esse. Se o nome mudou, PARAR e re-verificar antes de instalar.
    3. **package.json** (type: module) com deps exatas do RESEARCH §Standard Stack: `@modelcontextprotocol/server` ^2.1.0, `@modelcontextprotocol/express` ^2.0.1, `@modelcontextprotocol/node` ^2.1.0, `express` ^5.2.1, `zod` ^4.6.5, `better-sqlite3` ^13.0.3, `dayjs` ^1.11.23, `csv-parse` ^7.0.2; devDeps: `typescript` ^5.9.3, `tsx` ^4.23.15, `vitest` ^5.0.1, `@biomejs/biome` ^2.5.14, `@types/node`, `@types/express`, `@types/better-sqlite3`. Scripts: `dev` (tsx watch src/server.ts), `start` (tsx src/server.ts), `test` (vitest run), `check` (tsc --noEmit && biome check src tests).
    4. **tsconfig.json**: strict true, target ES2022, module NodeNext, noEmit true (tsx roda direto; o type-check é o `tsc --noEmit` do script check). **vitest.config.ts**: environment node, include tests. **biome.json** default.
    5. **src/lib/datas.ts**: dayjs com plugins utc+timezone (timezone depende de utc); constante `FUSO = 'America/Sao_Paulo'`; exports `agoraIsoUtc()` (new Date().toISOString()), `dataLocalSp(iso)` (dayjs(iso).tz(FUSO).format('YYYY-MM-DD')), `hojeLocalSp()`, `resolverDataLocal(data?: string)` — se `data` informada, validar regex YYYY-MM-DD e retornar como está; se ausente, retornar hojeLocalSp().
    6. **src/db/connect.ts**: caminho de `process.env.FLEXDIET_DB_PATH ?? 'data/flexdiet.db'`, mkdir recursivo do diretório, pragmas: journal_mode WAL, busy_timeout 5000, foreign_keys ON.
    7. **src/db/migrations/001_init.sql** — tabelas: `alimento` (id INTEGER PK AUTOINCREMENT, fonte TEXT NOT NULL DEFAULT 'taco', numero_taco TEXT, nome TEXT NOT NULL, kcal_100g REAL, proteina_g_100g REAL, carbo_g_100g REAL, gordura_g_100g REAL, criado_em TEXT; UNIQUE(fonte, numero_taco)); `medida_caseira` (id PK, alimento_id REFERENCES alimento, descricao TEXT NOT NULL, gramas REAL NOT NULL, UNIQUE(alimento_id, descricao)); `refeicao` (id PK, id_curto TEXT NOT NULL UNIQUE, data_local TEXT NOT NULL, timestamp_utc TEXT NOT NULL, tipo_refeicao TEXT, dedupe_hash TEXT); `refeicao_item` (id PK, refeicao_id REFERENCES refeicao ON DELETE CASCADE, alimento_id REFERENCES alimento, gramas REAL NOT NULL CHECK(gramas > 0), kcal REAL, proteina_g REAL, carbo_g REAL, gordura_g REAL — snapshot do débito no momento do registro); `meta_diaria` (id PK, data_inicio TEXT NOT NULL, kcal REAL NOT NULL, proteina_g REAL NOT NULL, carbo_g REAL NOT NULL, gordura_g REAL NOT NULL, timestamp_utc TEXT NOT NULL). Índice em refeicao(data_local) e refeicao(dedupe_hash).
    8. **src/db/migrate.ts**: carrega os .sql de src/db/migrations em ordem; lê `user_version`; para cada pendente: `await db.backup(...)` para `data/backups/pre-migration-{versao}-{timestamp}.db` FORA de transação (db.backup é promise e db.transaction rejeita async), depois db.exec(sql) e atualiza user_version. Retorna { aplicadas, versaoAtual }.
    9. **Vender o CSV**: `git clone --depth 1 https://github.com/brolesi/taco` em diretório temporário; copiar `data/processed/taco/taco_composicao.csv` para `src/db/seed/taco_composicao.csv`; remover o clone. (Dataset v1.7.0, MIT — vendido no repo conforme RESEARCH Wave 0.)
    10. **src/db/seed/seed.ts**: csv-parse/sync com { columns: true, skip_empty_lines: true }; mapear numero_alimento, descricao→nome, energia_kcal→kcal_100g (NUNCA energia_kj), proteina_g, carboidrato_g→carbo, lipideos_g→gordura; sanitizar: string vazia → NULL (não analisado), `1e-05` → 0 (Tr); upsert idempotente por (fonte='taco', numero_taco) via ON CONFLICT DO UPDATE em transação única síncrona (zero await dentro do closure); sanity in-code: banana maçã (numero_alimento '178') precisa ficar entre 85 e 89 kcal — lançar erro se falhar; retornar contagem { alimentos }.
    11. **src/domain/saldo.ts** — ÚNICA implementação do saldo (saldo nunca armazenado): `calcularSaldo(db, dataLocal)` agrega `SELECT COALESCE(SUM(kcal),0), COALESCE(SUM(proteina_g),0), COALESCE(SUM(carbo_g),0), COALESCE(SUM(gordura_g),0)` de refeicao_item JOIN refeicao WHERE refeicao.data_local = ?; busca metas vigentes (maior meta_diaria.data_inicio <= dataLocal); sem metas → `{ status: 'meta_nao_definida', instrucao: 'defina as metas com definir_metas informando kcal, proteina_g, carbo_g e gordura_g' }`; com metas → `{ status: 'ok', data, metas, consumido, restante }` (restante = meta − consumido; negativo permanece negativo, sem clampar).
    12. **src/domain/refeicoes.ts**: `registrarRefeicao(db, { data?, itens, tipoRefefeicao? })` — valida itens não vazio e gramas > 0; dentro de UMA transação SÍNCRONA (zero await no closure): resolve dataLocal via resolverDataLocal, valida cada alimento_id (inexistente → lançar erro de domínio alimento_nao_encontrado com sugestões), calcula macros do item = macro_100g × gramas/100 com COALESCE(NULL→0), gera id_curto com crypto.randomBytes(2).toString('hex') + retry dentro da transação em colisão no dia, INSERT refeicao + itens com snapshot. Retorna { registro: { id_curto, data_local, timestamp_utc, itens: [{ nome, gramas, fonte, kcal, proteina_g, carbo_g, gordura_g }] }, saldo } via calcularSaldo. Exportar também `gerarIdCurto`.
    13. **src/mcp/tools/registrar.ts** e **src/mcp/tools/saldo.ts**: wrappers finos — input zod (`zod/v4`): registrar_refeicao recebe `data?` (regex YYYY-MM-DD), `itens` array de `{ alimento_id: number inteiro positivo, gramas: number positivo com teto 5000 }` obrigatório (D-03 — gramas sempre; servidor não conhece unidades, D-04), `tipo_refeicao?`; consultar_saldo recebe `data?`. Ambos com annotations (readOnlyHint true em consultar_saldo) e resposta `{ content: [{ type: 'text', text: JSON.stringify(resultado) }], structuredContent: resultado }`; erros de domínio viram `isError: true` + código estruturado + texto acionável, nunca exceção crua.
    14. **src/mcp/server.ts**: exportar `buildServer: McpServerFactory` que cria `new McpServer({ name: 'flex-diet', version: '0.1.0' })` e registra registrar_refeicao e consultar_saldo (estrutura já preparada p/ registrar as tools seguintes nos plans 02/03).
    15. **src/server.ts**: verifier de token estático com crypto.timingSafeEqual contra `process.env.MCP_TOKEN` (OAuthError InvalidToken em caso contrário); `createMcpExpressApp({ host: '0.0.0.0', allowedHosts: process.env.PUBLIC_HOST ? [process.env.PUBLIC_HOST] : undefined })`; `app.all('/mcp', requireBearerAuth({ verifier, requiredScopes: ['mcp'] }), (req, res) => void nodeHandler(req, res, req.body))` — req.body como TERCEIRO argumento é obrigatório (express.json já consumiu o stream); `app.get('/healthz')` responde `{ ok: true }` sem auth; exportar `startServer({ port })` que roda runMigrations → runSeed → listen e retorna o server (para testes com porta 0); auto-executar só quando rodado direto. Log em stderr só de método/path/status, nunca de headers.
    16. **Testes** (vitest, db em tmpdir via FLEXDIET_DB_PATH):
        - tests/datas.test.ts: 2026-09-24T02:50:00Z → '2026-09-23'; 2026-09-24T03:10:00Z → '2026-09-24'; resolverDataLocal('2026-09-20') → '2026-09-20'; formato ISO do agoraIsoUtc.
        - tests/migrations.test.ts: user_version avança 0→1; arquivo de backup criado em data/backups/; rodar de novo = 0 aplicadas (no-op).
        - tests/seed.test.ts: sanity do ALIM-01 — banana maçã 85–89 kcal/100g; óleo de soja 880–888; nenhum alimento com kcal > 950 exceto gordura >= 90; `db.pragma('journal_mode', { simple: true }) === 'wal'`; re-seed não duplica (contagem estável).
        - tests/http.test.ts (o END-TO-END do tracer): startServer em porta 0 com MCP_TOKEN de teste; POST /mcp SEM Authorization → 401; POST /mcp com Bearer correto e Accept application/json, text/event-stream → initialize responde serverInfo flex-diet; tools/list lista registrar_refeicao e consultar_saldo; tools/call registrar_refeicao com 120g da banana maçã (id resolvido por numero_taco '178') → structuredContent com eco (nome, gramas 120, fonte taco), id_curto e saldo; em seguida tools/call registrar_refeicao com 150g do arroz tipo 2 (numero_taco 5) e consultar_saldo → consumido.kcal ≈ 195,18 (±0,5); GET /healthz → 200.
  </action>
  <verify>
    <automated>git check-ignore -v data/flexdiet.db && git check-ignore -v compose.env</automated>
    <fails_when>exit 1 em qualquer um dos dois check-ignore (caminho não ignorado — .gitignore ausente ou incompleto)</fails_when>
  </verify>
  <verify>
    <automated>npm view @modelcontextprotocol/server version</automated>
    <fails_when>exit não-zero (nome do pacote MCP v2 não existe mais no registry — parar e re-verificar legitimidade)</fails_when>
  </verify>
  <verify>
    <automated>npm --prefix . install</automated>
    <fails_when>exit não-zero ou bloco "npm ERR!" na saída</fails_when>
  </verify>
  <verify>
    <automated>npx vitest run tests/datas.test.ts tests/migrations.test.ts tests/seed.test.ts tests/http.test.ts</automated>
    <fails_when>exit não-zero, ou qualquer linha "failed" maior que zero, ou "0 passed" no resumo do vitest</fails_when>
  </verify>
  <verify>
    <automated>npm run check</automated>
    <fails_when>exit não-zero (erros de tipo do tsc ou de lint do biome listados na saída)</fails_when>
  </verify>
  <done>Uma chamada autenticada de registrar_refeicao pela rota /mcp debita macros do catálogo TACO e retorna eco + id_curto + saldo na mesma resposta; sem token é 401; SQLite em WAL com migration + backup pré-migration; seed passa no sanity do ALIM-01; datas duplas corretas na fronteira de meia-noite de São Paulo.</done>
  <acceptance_criteria>
    - `.gitignore` cobre data/, compose.env e *.db — `git check-ignore` sai 0 para data/flexdiet.db e compose.env
    - `package.json` declara @modelcontextprotocol/server ^2.1.0, better-sqlite3 ^13.0.3, express ^5.2.1, zod ^4.6.5 e script test = vitest run
    - `src/db/migrations/001_init.sql` contém CREATE TABLE para alimento, medida_caseira, refeicao, refeicao_item e meta_diaria, com UNIQUE(fonte, numero_taco) e CHECK(gramas > 0)
    - `src/db/migrate.ts` contém chamada await de db.backup antes de db.exec e escrita de user_version
    - `src/db/seed/seed.ts` lê a coluna energia_kcal (não a de kJ), mapeia vazio → NULL e Tr → 0, e contém o sanity da banana (85–89)
    - `src/domain/saldo.ts` contém a única agregação de saldo do codebase (SUM sobre refeicao_item por data_local) e o ramo meta_nao_definida com instrução
    - `src/server.ts` contém createMcpExpressApp com host 0.0.0.0, requireBearerAuth com timingSafeEqual, rota /healthz e passa req.body como 3º argumento do node handler
    - tests/http.test.ts prova: 401 sem token; registrar 120g banana → eco + id_curto + saldo; 150g arroz → ~195,18 kcal no saldo
    - tests/seed.test.ts prova: banana maçã 85–89, óleo 880–888, journal_mode wal, re-seed idempotente
    - Suíte vitest verde e npm run check (tsc --noEmit + biome) sem erros
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: definir_metas com os 4 valores obrigatórios (D-10) fechando o ciclo do saldo (META-01)</name>
  <files>src/domain/metas.ts, src/mcp/tools/metas.ts, src/mcp/server.ts, tests/metas.test.ts</files>
  <read_first>
    src/domain/saldo.ts
    src/mcp/server.ts
    src/db/migrations/001_init.sql
    .planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-RESEARCH.md
  </read_first>
  <action>
    1. **src/domain/metas.ts**: `definirMetas(db, { kcal, proteina_g, carbo_g, gordura_g })` — valida os 4 números positivos e finitos; INSERT em meta_diaria com data_inicio = hojeLocalSp() e timestamp_utc = agoraIsoUtc() (vigência por data_inicio já prepara a aderência histórica da Phase 3, META-04). `obterMetasVigentes(db, dataLocal)` — a mesma consulta que calcularSaldo usa (extraia daí para não duplicar: saldo.ts passa a importar de metas.ts, mantendo UMA implementação da regra de vigência).
    2. **src/mcp/tools/metas.ts**: tool `definir_metas` com inputSchema zod exigindo OS 4 CAMPOS como required (D-10 — sem atualização parcial; "meta 1800kcal" sozinho NÃO é chamada válida, o Hermes coleta os 4 antes de chamar). Resposta: metas definidas + saldo do dia recalculado via calcularSaldo (mesma resposta, D-05).
    3. **src/mcp/server.ts**: registrar a tool definir_metas no factory (padrão idêntico às tools da Task 1).
    4. **tests/metas.test.ts**: (a) schema rejeita chamada com apenas 3 valores (parse lança/retorna erro); (b) sem metas, calcularSaldo retorna status meta_nao_definida com instrucao (D-09); (c) definir 1800/150/180/60 → obterMetasVigentes retorna os 4 valores; (d) registrar uma refeição APÓS definir metas → saldo.status ok e restante = meta − consumido; (e) nova chamada de definir_metas substitui a vigente (última data_inicio vence) e o próximo saldo usa os valores novos.
  </action>
  <verify>
    <automated>npx vitest run tests/metas.test.ts</automated>
    <fails_when>exit não-zero, ou qualquer teste com "failed" no resumo do vitest</fails_when>
  </verify>
  <verify>
    <automated>npx vitest run</automated>
    <fails_when>exit não-zero, ou "failed" maior que zero, ou "0 passed" no resumo</fails_when>
  </verify>
  <verify>
    <automated>npm run check</automated>
    <fails_when>exit não-zero (erros de tipo ou lint)</fails_when>
  </verify>
  <done>Sem metas o saldo responde meta_nao_definida com instrução; definir_metas com os 4 valores grava vigência por data_inicio e o saldo recalcula na mesma resposta; chamada com 3 valores é rejeitada pelo schema.</done>
  <acceptance_criteria>
    - `src/mcp/tools/metas.ts` declara kcal, proteina_g, carbo_g e gordura_g todos required no inputSchema (D-10)
    - `src/domain/metas.ts` grava data_inicio + timestamp_utc em meta_diaria e expõe obterMetasVigentes usado por saldo.ts (uma única regra de vigência)
    - tests/metas.test.ts cobre: 3 valores rejeitados, meta_nao_definida antes de definir, saldo correto após definir, substituição da meta vigente
    - Suíte completa vitest verde
  </acceptance_criteria>
</task>

</tasks>

<assumption_delta_decision>
Termo disparado: "fallback" (SDK MCP v2 → v1). Decisão: **no-change**. Racional: é fallback de ERA DE PROTOCOLO na negociação de transporte do primeiro contato (A2/INFRA-01) — contingência de pacote, não uma segunda identidade/plataforma armazenada; nenhuma tabela, tool ou contrato passa a existir em forma plural por causa disso.
</assumption_delta_decision>

## Flagged Assumptions (edge-probe, não resolvidos — nunca drop silencioso)

| Requisito | Assunção explícita desta fase |
|---|---|
| INFRA-02 | Nenhuma edge além das cobertas (WAL assert, user_version, backup pré-migration, no-op idempotente) — o runner é determinístico e single-boot; corrida de dois boots simultâneos no MESMO volume não é cenário alvo (compose mantém 1 container). |
| INFRA-03 | Nenhuma edge além da fronteira 23h50→00h10 testada — America/Sao_Paulo é offset fixo −03 (DST abolido em 2019, A4). |
| INFRA-04 | Privacidade coberta por gitignore-first + volume local + proibição de egress; não há edge de "dados parcialmente fora" além disso. |
| ALIM-01 | Valores especiais do TACO (vazio = não analisado, Tr = 1e-05) seguem o dicionário do dataset; sanity cobre o resto. |
| REG-01 | A interpretação "linguagem natural → itens" é 100% do Hermes; edge de parsing no servidor não existe por decisão de projeto. |
| REG-02 | Eco de fonte cobre apenas 'taco' nesta fase (OFF/estimativa são Phase 2). |
| REG-05 | Retroatividade aceita para qualquer data YYYY-MM-DD passada informada explicitamente; não há limite de quantos dias para trás nesta fase. |
| META-01 | Metas são globais do usuário único (sem múltiplos perfis); vigência por data_inicio já antecipa META-04 sem custo. |

## Artifacts this phase produces

Símbolos criados por ESTE plano (parte do conjunto da fase):
- **Arquivos**: `.gitignore`, `package.json`, `tsconfig.json`, `vitest.config.ts`, `biome.json`, `src/lib/datas.ts`, `src/db/connect.ts`, `src/db/migrate.ts`, `src/db/migrations/001_init.sql`, `src/db/seed/seed.ts`, `src/db/seed/taco_composicao.csv`, `src/domain/saldo.ts`, `src/domain/refeicoes.ts`, `src/domain/metas.ts`, `src/mcp/server.ts`, `src/mcp/tools/registrar.ts`, `src/mcp/tools/saldo.ts`, `src/mcp/tools/metas.ts`, `src/server.ts`, `tests/*.test.ts` (5 arquivos)
- **Funções/exports**: `agoraIsoUtc`, `dataLocalSp`, `hojeLocalSp`, `resolverDataLocal`, `FUSO`, `calcularSaldo`, `registrarRefeicao`, `gerarIdCurto`, `definirMetas`, `obterMetasVigentes`, `buildServer` (McpServerFactory), `startServer`, `runMigrations`, `runSeed`, `connectDb`
- **Tools MCP**: `registrar_refeicao`, `consultar_saldo`, `definir_metas`
- **Tabelas SQLite**: `alimento`, `medida_caseira`, `refeicao`, `refeicao_item`, `meta_diaria`
- **Env vars**: `MCP_TOKEN`, `PORT`, `PUBLIC_HOST`, `FLEXDIET_DB_PATH`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hermes/Internet → /mcp | Requisições não autenticadas chegam pela porta exposta da VPS |
| Tool input → SQLite | Itens/gramas/data vindos do LLM do Hermes cruzam para queries |
| Env (MCP_TOKEN) → verifier | Token comparação secreta |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Spoofing | POST /mcp | high | mitigate | requireBearerAuth + verifier com crypto.timingSafeEqual; 401 sem token (testado em tests/http.test.ts); token só do env, nunca no git |
| T-01-02 | Tampering | Tools → queries SQL | high | mitigate | Prepared statements em 100% das queries better-sqlite3; nenhum input concatenado em SQL |
| T-01-03 | Info Disclosure | Logs do processo | medium | mitigate | Log só de método/path/status em stderr; headers (authorization) nunca logados |
| T-01-04 | DoS | body do POST /mcp | low | mitigate | jsonLimit default (100kb) do createMcpExpressApp + teto de 5000g por item no zod; single-user = superfície mínima |
| T-01-05 | Tampering | Dados de saúde via git | high | mitigate | .gitignore criado ANTES do 1º commit (data/, compose.env, *.db) — primeiro verify da Task 1 |
| T-01-06 | DoS | DNS rebinding / Host header | medium | mitigate | allowedHosts do createMcpExpressApp com PUBLIC_HOST; cliente não-navegador não envia Origin e passa |
| T-01-SC | Tampering | npm install (supply chain) | high | mitigate | Package Legitimacy Audit completo no RESEARCH (todos Approved; [SUS: too-new] com repo/scope oficiais e downloads verificados) + gate pré-install da Task 1 (npm view + README oficial) antes do primeiro npm install |
</threat_model>

<verification>
- `npx vitest run` — suíte inteira verde (datas, migrations, seed, http, metas)
- `npm run check` — tsc --noEmit + biome sem erros
- `git check-ignore` cobre data/ e compose.env
- Smoke manual documentado no SUMMARY: curl do RESEARCH §Code Example 1 contra o servidor local (initialize + tools/list)
</verification>

<success_criteria>
- Loop "registrei → saldo" funciona end-to-end sobre HTTP MCP autenticado (o teste http boot o servidor, chama as tools e verifica saldo com valores reais do TACO)
- Contratos de confiança nascidos: eco item+gramas+fonte, id_curto, datas duplas, meta_nao_definida sem default, 4 valores obrigatórios em definir_metas
- Infra de dados verificável: WAL, migration + backup pré-migration idempotentes, seed TACO são (banana 86,8 kcal — não kJ)
- Padrões estabelecidos para as fases 2–5 (estrutura de pastas, erro estruturado, transação síncrona, prepared statements)
</success_criteria>

<output>
Create `.planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-01-SUMMARY.md` when done
</output>
