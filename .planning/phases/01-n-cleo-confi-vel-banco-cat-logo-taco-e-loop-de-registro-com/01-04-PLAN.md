---
phase: 01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - Dockerfile
  - .dockerignore
  - docker-compose.yml
  - compose.env.example
  - scripts/check-deploy-contract.mjs
  - src/db/backup.ts
  - src/server.ts
  - tests/backup.test.ts
autonomous: false
user_setup:
  - service: VPS pessoal + GitHub secrets
    why: "Deploy contínuo (INFRA-05): o workflow deploy.yml pula silenciosamente sem os secrets, e o compose precisa do compose.env com MCP_TOKEN na VPS para subir com o endpoint protegido"
    env_vars:
      - name: VPS_HOST / VPS_USER / VPS_PORT / VPS_PATH / VPS_SSH_KEY
        source: "GitHub → Settings → Secrets and variables → Actions (dados da sua VPS)"
    dashboard_config:
      - task: "Clonar este repo em VPS_PATH na VPS e confirmar docker + plugin compose instalados (docker compose version)"
        location: "SSH na VPS"
      - task: "Criar compose.env ao lado do compose na VPS com MCP_TOKEN (32+ bytes aleatórios), PORT e PUBLIC_HOST"
        location: "SSH na VPS — compose.env fica fora do git"
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "docker-compose.yml + Dockerfile na RAIZ do repo satisfazem o contrato que deploy.yml executa na VPS (compose na raiz; git pull --ff-only + docker compose up -d --build sobem o serviço com healthcheck /healthz)"
    - "deploy.yml declara grupo de concurrency (GitHub Actions); dois merges quase simultâneos geram deploys serializados — nunca dois deploys paralelos na mesma VPS"
    - statement: "Um run de deploy sem mudança de código termina sem deixar o serviço fora do ar (serviço continua respondendo após um deploy vazio)"
      verification: backstop
    - statement: "A VPS nunca regride para versão mais antiga que a já implantada (git pull --ff-only; versões aplicam-se em ordem de commit)"
      verification: backstop
    - "Imagem do container: node:22-slim (nunca alpine — better-sqlite3 sem prebuild musl); healthcheck do compose aponta para /healthz; volume ./data:/app/data persiste o .db entre deploys; compose.env fora do git"
    - "tsx resolve em produção dentro do container com npm ci --omit=dev (tsx nas dependencies, não devDependencies)"
    - "Backup diário do .db via db.backup() (snapshot consistente em WAL) para data/backups/ com retenção de 7 dias (D-02), além do backup pré-migration já existente"
    - "1º deploy real observado: run do Actions verde com o step de deploy EXECUTADO na VPS (não o notice de pulado por falta de secrets) e GET /healthz respondendo na VPS"
  artifacts:
    - path: "Dockerfile"
      provides: "Imagem node:22-slim com npm ci --omit=dev e CMD via tsx local (sem download em runtime)"
    - path: "docker-compose.yml"
      provides: "Contrato do deploy.yml: build, ports, env_file, volume ./data:/app/data, healthcheck /healthz, restart unless-stopped"
    - path: "compose.env.example"
      provides: "Modelo de MCP_TOKEN/PORT/PUBLIC_HOST (o real só existe na VPS)"
    - path: "scripts/check-deploy-contract.mjs"
      provides: "Check versionado do contrato: compose+Dockerfile na raiz, data/ e compose.env gitignored"
    - path: "src/db/backup.ts"
      provides: "fazerBackupDiario (db.backup) + reterUltimos (retenção 7) + agendarBackupDiario (job interno)"
  key_links:
    - from: "docker-compose.yml"
      to: ".github/workflows/deploy.yml"
      via: "o workflow procura docker-compose.yml na raiz do VPS_PATH e roda docker compose up -d --build"
      pattern: "docker-compose"
    - from: "src/server.ts"
      to: "src/db/backup.ts"
      via: "boot agenda o backup diário com retenção 7"
      pattern: "agendarBackupDiario"
---

<objective>
Publicar o serviço: Dockerfile + docker-compose.yml no contrato exato que `.github/workflows/deploy.yml` já consome, backup diário com retenção (D-02), e o 1º deploy real na VPS observado de ponta a ponta (merge na main → serviço novo no ar sem intervenção manual).

Purpose: INFRA-05 é o que torna todas as fases seguintes contínuas; os riscos aqui são operacionais (secrets ausentes geram deploy silenciosamente pulado — RESEARCH Pitfall 8).
Output: Serviço rodando na VPS por deploy automático; backup diário ativo; contrato de deploy versionado e checável.

## Phase Goal

**As a** dono do Flex Diet, **I want to** que cada merge na main publique a versão nova na minha VPS sem eu mexer em nada, **so that** o sistema evolua sem fricção de operação.
</objective>

<execution_context>
@C:/Users/guilhermino.araujo/.zcode/gsd-core/workflows/execute-plan.md
@C:/Users/guilhermino.araujo/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-CONTEXT.md
@.planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-RESEARCH.md
@.github/workflows/deploy.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dockerfile + docker-compose.yml no contrato do deploy.yml + check versionado</name>
  <files>Dockerfile, .dockerignore, docker-compose.yml, compose.env.example, scripts/check-deploy-contract.mjs</files>
  <read_first>
    .github/workflows/deploy.yml
    .planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-RESEARCH.md
    package.json
    .gitignore
  </read_first>
  <action>
    1. **Dockerfile**: FROM node:22-slim (NUNCA alpine — better-sqlite3 não tem prebuild musl e o build de fonte inflaria o deploy a cada merge; RESEARCH Pitfall 5); WORKDIR /app; COPY package.json package-lock.json; RUN npm ci --omit=dev; COPY tsconfig.json e src/; ENV NODE_ENV=production; CMD usando o binário tsx local (./node_modules/.bin/tsx src/server.ts). Pré-requisito desta decisão: tsx precisa estar nas **dependencies** (não devDependencies) do package.json — movê-lo e regenerar o lock, pois --omit=dev não pode remover o runner do processo de produção.
    2. **.dockerignore**: node_modules, data, .git, .planning, compose.env, tests (imagem não leva dados nem segredos).
    3. **docker-compose.yml** na RAIZ (nome exato que o deploy.yml procura): serviço flex-diet com build: ., restart: unless-stopped, ports "${PORT:-8787}:8787", env_file: compose.env (MCP_TOKEN, PORT, PUBLIC_HOST — o arquivo real só existe na VPS), volumes "./data:/app/data", healthcheck com test usando node -e com fetch ao http://127.0.0.1:8787/healthz saindo 0/1 conforme r.ok (interval 30s, timeout 5s, retries 3). Não publicar nenhuma outra porta.
    4. **compose.env.example**: MCP_TOKEN= (com comentário de gerar 32+ bytes aleatórios), PORT=8787, PUBLIC_HOST= (domínio/ip pelo qual o Hermes alcança a VPS — alimenta allowedHosts do servidor, RESEARCH Pitfall 4).
    5. **scripts/check-deploy-contract.mjs** (node puro, sem deps): falha (exit 1, mensagem clara) se: docker-compose.yml OU compose.yaml não existir na raiz; Dockerfile não existir; compose.env.example não existir; .gitignore não cobrir data/ (usa git check-ignore) ou não cobrir compose.env; senão imprime "contrato de deploy ok" e exit 0. Replica localmente o check que o deploy.yml faz na VPS.
    6. Rodar `docker compose build` localmente (Docker 28 disponível) para provar a imagem; sem `up` local obrigatório (não há compose.env local — criar um temporário FORA do git apenas se quiser provar o boot, e removê-lo depois).
  </action>
  <verify>
    <automated>node scripts/check-deploy-contract.mjs</automated>
    <fails_when>exit não-zero com mensagem de item de contrato ausente (compose/Dockerfile/gitignore)</fails_when>
  </verify>
  <verify>
    <automated>docker compose build</automated>
    <fails_when>exit não-zero, ou linhas de ERROR no output do build, ou node-gyp/python no log (sinal de alpine/build de fonte)</fails_when>
  </verify>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies||!p.dependencies.tsx){console.error('tsx fora das dependencies');process.exit(1)}"</automated>
    <fails_when>exit não-zero com a mensagem de tsx fora das dependencies</fails_when>
  </verify>
  <done>Contrato de deploy satisfeito e versionado; imagem node:22-slim constrói localmente sem compilar better-sqlite3 de fonte; nenhum segredo no git.</done>
  <acceptance_criteria>
    - `docker-compose.yml` na raiz com build: ., volume ./data:/app/data, env_file compose.env e healthcheck apontando a /healthz
    - `Dockerfile` começa com node:22-slim, usa npm ci --omit=dev e o CMD resolve tsx de node_modules (sem npx que baixa pacote em runtime)
    - `compose.env.example` lista MCP_TOKEN, PORT e PUBLIC_HOST; `compose.env` real está gitignored
    - `scripts/check-deploy-contract.mjs` sai 0 no estado atual e sai 1 se o compose for renomeado (prova rápida: renomear, rodar, restaurar — opcional)
    - docker compose build verde localmente
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Backup diário com retenção 7 (D-02)</name>
  <files>src/db/backup.ts, src/server.ts, tests/backup.test.ts</files>
  <read_first>
    src/server.ts
    src/db/migrate.ts
    src/db/connect.ts
    .planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-CONTEXT.md
    .planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-RESEARCH.md
  </read_first>
  <action>
    1. **src/db/backup.ts**: `fazerBackupDiario(db, dir)` — await db.backup(caminho `dir/diario-YYYY-MM-DD-HHmmss.db`) (promise; snapshot consistente mesmo em WAL — nunca cópia crua do arquivo, RESEARCH Pitfall 6); `reterUltimos(dir, maximo)` — função PURA de decisão: lista os arquivos diario-*.db ordenados por nome (timestamps ordenáveis) e retorna os que devem ser removidos além dos 7 mais recentes; `aplicarRetencao(dir, maximo)` — executa a remoção com fs.unlinkSync; `agendarBackupDiario(db, dir)` — roda uma vez no boot e reagendar a cada 24h (timer que não segura o processo: unref se disponível).
    2. **src/server.ts**: no boot (após migrations + seed), chamar agendarBackupDiario apontando para o diretório de backups ao lado do banco (data/backups). Erro do backup diário NÃO derruba o serviço: log em stderr e segue.
    3. **tests/backup.test.ts**: (a) fazerBackupDiario cria arquivo legível (abrir a cópia com better-sqlite3 e ler user_version); (b) reterUltimos com 9 arquivos mantém 7 e aponta exatamente os 2 mais antigos (função pura — teste direto com tmpdir); (c) aplicarRetencao remove só os apontados; (d) teste de restauração barato: abrir o backup criado e consultar uma tabela conhecida (prova que o snapshot é consistente, não um arquivo vazio).
  </action>
  <verify>
    <automated>npx vitest run tests/backup.test.ts</automated>
    <fails_when>exit não-zero, ou qualquer teste "failed", ou "0 passed" no resumo do vitest</fails_when>
  </verify>
  <verify>
    <automated>npx vitest run</automated>
    <fails_when>exit não-zero, ou "failed" maior que zero, ou "0 passed" no resumo</fails_when>
  </verify>
  <done>Backup diário db.backup() com retenção 7 ativo no boot; restauração provada em teste (backup abre e lê).</done>
  <acceptance_criteria>
    - `src/db/backup.ts` usa db.backup (nunca cópia fs do .db em WAL) e retenção pura de 7
    - `src/server.ts` agenda o backup diário no boot sem derrubar o serviço em caso de erro
    - tests/backup.test.ts prova: arquivo criado + legível por better-sqlite3, retenção exata de 7, restauração consulta dados
    - Suíte completa vitest verde
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: 1º deploy real na VPS (merge na main → serviço no ar)</name>
  <precondition>Secrets VPS_HOST/VPS_USER/VPS_PORT/VPS_PATH/VPS_SSH_KEY configurados no GitHub; repo clonado em VPS_PATH na VPS; docker compose disponível na VPS; compose.env criado na VPS com MCP_TOKEN/PORT/PUBLIC_HOST (user_setup deste plano)</precondition>
  <files></files>
  <read_first>
    .github/workflows/deploy.yml
    scripts/check-deploy-contract.mjs
    .planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-RESEARCH.md
  </read_first>
  <what-built>1º deploy real do Flex Diet na VPS: Dockerfile + docker-compose.yml no contrato do deploy.yml, push/merge na main feito pelo executor, backup diário ativo — falta a confirmação humana do run do Actions e do /healthz na VPS</what-built>
  <how-to-verify>
    Executor faz antes do checkpoint: rodar node scripts/check-deploy-contract.mjs (exit 0) e commitar/push na main.

    A pessoa confirma (ou faz, se ainda não feito) e responde:
    1. Secrets VPS_HOST/VPS_USER/VPS_PORT/VPS_PATH/VPS_SSH_KEY no GitHub (Settings → Secrets and variables → Actions) — sem eles o deploy é PULADO silenciosamente.
    2. Na VPS: repo clonado em VPS_PATH; `docker compose version` OK; compose.env criado ao lado do compose com MCP_TOKEN (32+ bytes aleatórios), PORT e PUBLIC_HOST; firewall liberando só a porta necessária.
    3. O run do Actions da main terminou VERDE com o step de deploy EXECUTADO — na VPS, `docker compose ps` mostra o container flex-diet healthy? (Se apareceu o notice de deploy pulado: secrets ausentes — corrigir e repetir; nunca declarar INFRA-05 completo com deploy pulado.)
    4. GET http://&lt;vps&gt;:&lt;porta&gt;/healthz responde {"ok":true} de fora? (prova o 1º deploy real — INFRA-05)
    5. Deploy vazio: re-executar o workflow (ou push de docs) mantém o serviço respondendo? (backstop: deploy sem mudança não derruba o serviço)
    6. Sem regressão: após um 2º merge, a VPS roda a versão NOVA (git pull --ff-only termina ok no log do Actions)? (backstop de ordenação)
  </how-to-verify>
  <resume-signal>Responder "aprovado" confirmando os itens 1-6, ou descrever qual item falhou (o executor instrui a correção e o checkpoint se repete)</resume-signal>
  <verify>
    <automated>node scripts/check-deploy-contract.mjs</automated>
    <fails_when>exit não-zero (contrato quebrado antes do push)</fails_when>
  </verify>
  <done>Usuário confirma: Actions verde com deploy executado, /healthz da VPS respondendo, deploy vazio não derruba o serviço, e a VPS não regride de versão. O resultado (incluindo observações de operação como firewall/TLS pendentes) fica registrado no SUMMARY.</done>
  <acceptance_criteria>
    - Run do Actions na main com deploy executado (sem o notice de pulado) — confirmado pela pessoa no checkpoint
    - /healthz responde na VPS a partir de fora — confirmado pela pessoa
    - Backstops observados: deploy vazio mantém o serviço no ar; VPS não regride de versão
    - SUMMARY registra data/hora do 1º deploy e a porta/pública usada (sem segredos)
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

Símbolos criados por ESTE plano (complementam os dos plans 01–03):
- **Arquivos**: `Dockerfile`, `.dockerignore`, `docker-compose.yml`, `compose.env.example`, `scripts/check-deploy-contract.mjs`, `src/db/backup.ts`, `tests/backup.test.ts`
- **Funções/exports**: `fazerBackupDiario`, `reterUltimos`, `aplicarRetencao`, `agendarBackupDiario`
- **Env vars**: MCP_TOKEN, PORT, PUBLIC_HOST (consumidos pelo compose/Dockerfile)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → SSH → VPS | Chave privada trafega pelo secret e executa comandos remotos |
| Internet → porta publicada | Endpoint exposto além do Hermes |
| Git ↔ dados de saúde | Risco de dados pessoais entrarem no repositório |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-13 | Info Disclosure | Dados de saúde no git | high | mitigate | data/ e compose.env gitignored (verificado por scripts/check-deploy-contract.mjs e .dockerignore); repo privado |
| T-01-14 | Tampering | Migration destrutiva sem volta | high | mitigate | Backup db.backup() pré-migration (plan 01) + diário com retenção 7 (Task 2); restauração provada em teste |
| T-01-15 | Info Disclosure | Token em HTTP puro na internet | medium | accept | MVP: HTTP + token de alta entropia + firewall; decisão de TLS (reverse proxy existente?) fica no checkpoint do Hermes (plan 05) — registrada como risco aceito até lá |
| T-01-16 | DoS | Porta aberta além do necessário | medium | mitigate | Firewall da VPS restringindo a porta (instrução do checkpoint); jsonLimit + single-user reduzem superfície |
| T-01-17 | Spoofing | Deploy silenciosamente pulado dando falsa sensação de publicação | medium | mitigate | Checkpoint exige o step EXECUTADO (não o notice de pulado) e smoke /healthz na VPS |
| T-01-SC | Tampering | npm ci dentro do build do container | medium | mitigate | package-lock.json gerado do install já auditado (plan 01); build usa o lock congelado — nenhum pacote novo sem passar pelo gate de legitimidade |
</threat_model>

<verification>
- `node scripts/check-deploy-contract.mjs` sai 0
- `docker compose build` verde local (sem node-gyp no log)
- `npx vitest run` suíte completa verde (inclui backup)
- 1º deploy real: Actions verde com deploy executado + /healthz na VPS (confirmado no checkpoint)
</verification>

<success_criteria>
- Merge na main publica a versão nova na VPS sem intervenção manual (INFRA-05), com os dois backstops observados (deploy vazio não derruba; sem regressão de versão)
- Dados sobrevivem ao deploy: volume ./data persiste o .db entre builds (usuário re-registra nada após redeploy)
- D-02 ativo: backup diário com retenção 7 além do pré-migration
</success_criteria>

<output>
Create `.planning/phases/01-n-cleo-confi-vel-banco-cat-logo-taco-e-loop-de-registro-com/01-04-SUMMARY.md` when done
</output>
